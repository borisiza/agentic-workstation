---
scope: Agentic Workstation MVP: symmetric multi-node mesh (5+ nodes) over Tailscale, any node -> any node SSH into tmux-hosted Claude Code; public repo
purpose: build-substrate
altitude: feature
updated: 2026-09-10T13:48
---

- (direction) Fast path (owner: YOLO for the whole cycle); spine is the only deliverable; conversation in Spanish, artifacts in English (document_output_language).
- (constraint) Inherited from PRD (final, PR #5): Tailscale tailnet only, Tailscale SSH primary (FR1), OpenSSH-over-tailnet key-only fallback opt-in per host (FR18), node role in gitignored config/local.env (FR17), tmux [ASSUMPTION] pending OQ5, no host registry (FR13/NFR6), public repo hygiene FR11-13, no --dangerously-skip-permissions (FR5), workspace allowlist (FR16).
- (version) Verified 2026-09-10: Tailscale v1.102.3 (github releases; Homebrew formula 'tailscale' same version, 'brew install tailscale' + 'sudo brew services start tailscale'); tmux 3.7c (2026-08-17); gitleaks v8.30.1 (2026-03-21, feature-complete, security patches only); shellcheck v0.11.0 (2025-08-04); mosh 1.4.0 (2024-10-27, last release); Claude Code native installer 'curl -fsSL https://claude.ai/install.sh | bash', auto-updating, docs reference >=2.1.257 for background-session features.
- (decision) OQ5 resolved: tmux stays the persistence primitive (attach = same running process, no context replay). Claude Code native --resume/--continue restores conversation but NOT an in-flight tool run; --bg/supervisor/claude attach is research-preview agent-view. Convention: 'claude -n claude-<workspace>' so a tmux server loss (host reboot) recovers via 'claude --resume claude-<workspace>'. Sources: code.claude.com/docs/en/sessions, /agent-view, /cli-reference.
- (decision) OQ4 resolved: mosh NOT adopted for MVP. tmux already holds session state; reconnect = one ssh + tmux attach (<1 min, NFR2). mosh would add a UDP 60000-61000 path and a second server binary per host for latency-only gains; last release 1.4.0 (2024). Mitigation retained: ssh ServerAliveInterval=15 / ServerAliveCountMax=3 in the client config template.
- (decision) OQ6 resolved (Windows host = WSL2 tailnet node): tailscaled inside the distro under systemd (/etc/wsl.conf [boot] systemd=true; tailscale up --ssh). WSL stays in default NAT networking (own netns, no port-22 clash with the Windows client; mirrored mode rejected). Keep-alive: %USERPROFILE%\.wslconfig [wsl2] vmIdleTimeout=-1 + instanceIdleTimeout=-1 (WSL>=2.4.4; MS docs: systemd services alone do NOT keep the instance alive) + a Task Scheduler at-logon task 'wsl.exe -d <distro> --exec sleep infinity'; Windows auto-login/lock + powercfg no-sleep documented as host prerequisites. Known caveat: tailscale/tailscale#6189 (Tailscale SSH hang in WSL2, workaround MTU 1200 on tailscale0) -> doctor.sh check. One tailnet node per PC-WSL pair (multiple distros would fight over the TUN device).
- (decision) OQ7 resolved: macOS host runs the open-source tailscaled variant (Homebrew formula, launchd via brew services, root). It replaces the Standalone/App Store app on that Mac (one tailscaled per machine); loss of GUI accepted for hosts. A Mac that must keep the GUI app is a 'client' node or uses the FR18 OpenSSH fallback (Remote Login on, key-only, sshd reachable only via tailnet ACL, not by ListenAddress pinning). Tailscale SSH server support: Linux + macOS open-source only (tailscale.com/docs/features/tailscale-ssh).
- (constraint) Tailscale SSH policy: default tailnet rule ssh[{action:check, src:autogroup:member, dst:autogroup:self, users:[autogroup:nonroot, root]}] already permits owner->own devices; hosts still opt in per device with 'tailscale up --ssh'. Repo commits NO policy file; docs show the default block only. Port 22 is claimed by tailscaled on the tailnet IP; restart of tailscaled drops live SSH sessions (tmux survives).
- (decision) Scripts: bash targeting bash 3.2 (macOS default) — no associative arrays, no ${var,,}; 'set -euo pipefail'; shellcheck in CI. Discovery parses 'tailscale status' text columns (no jq dependency). Secret scanning: gitleaks pre-commit ('gitleaks git --pre-commit --staged') + gitleaks-action on push. No bats: each script exposes '--check' self-test (ponytail).
- (assumption) [ASSUMPTION] Claude Code version is not pinned (native installer auto-updates); floor recorded as 2.1.257 (oldest version the cited docs require). Revisit if a breaking CLI flag change lands.
- (event) Research done inline (Agent hook in .claude/settings.json rejects every subagent call in this build: it requires tool_input.run_in_background=false but the Agent schema strips the field). Reviewer Gate will run inline for the same reason.
- (event) spine finalized — Reviewer Gate (inline): rubric+adversarial 3 high/1 medium/2 low fixed (role gating, repo path $HOME/agentic-workstation, explicit --fallback, workspace resolution); versions PASS (WSL>=2.4.4 tagged [ASSUMPTION]); security PASS (transcripts outside repo, stop.sh single-session, no root). lint_spine 0 findings. status: final.
